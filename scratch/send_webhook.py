import requests
import os
import json

url = "https://dwepduvhzuhzeehbeaaz.supabase.co" # No, the webhook is deployed to netlify or we can hit the local function
# Wait, I don't need to hit the remote, I can just call the netlify function locally if it's running, or run the file.
